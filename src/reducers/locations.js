const defaultState = {
  mapCenter: {
    lat: 43.071584,
    lng: -89.380120,
  },
  locations: []
};

const locations = (state = defaultState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default locations;
